const SectionDeviderWrapper = ({ children }) => {
  return (
    <div className="my-4 border-t border-b border-black w-full py-2 flex items-center justify-center">
      {children}
    </div>
  );
};

export default SectionDeviderWrapper;
